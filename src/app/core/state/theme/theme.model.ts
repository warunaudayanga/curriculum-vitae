export interface ThemeStateModel {
    name: string;
    primaryColor: string;
    secondaryColor: string;
    accentColor: string;
    textColor: string;
    separatorColor: string;
    mainLinkColor: string;
}
